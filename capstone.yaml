run: docker compose up --build --wait
seed: docker compose --profile seed run --rm --no-deps demo-seed
test: .venv/bin/pytest -q
base_url: http://localhost:8000
endpoints:
  - /health
  - /api/public/widgets/{id}/config
  - /api/public/widgets/{id}/submissions
  - /api/dashboard/analytics
